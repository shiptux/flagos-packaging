name: flagscale
upstream: flagos-ai/FlagScale
display_name: FlagScale
workflows:
  deb:
    name: build-deb.yml
    artifact_pattern: "flagscale-*-packages"
  rpm:
    name: build-rpm.yml
    artifact_pattern: "flagscale-*-rpm-packages"
notes: |
  Pure Python (Architecture: all). Packaging branch pr/packaging committed
  2026-04-26 (commit c0be689); upstream PR not yet opened, so the
  artifact source path may shift once the PR is merged.
