name: libtriton-jit
upstream: flagos-ai/libtriton_jit
display_name: libtriton-jit
workflows:
  deb:
    name: build-deb.yml
    artifact_pattern: "libtriton-jit-*-packages"
  rpm:
    name: build-rpm.yml
    artifact_pattern: "libtriton-jit-.*rpm-packages"
notes: |
  Native C++ runtime helper used by FlagTree's JIT compilation path.
  Architecture: amd64. Has its own dev split (libtriton-jit-dev for
  headers).
